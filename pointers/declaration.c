#include <stdio.h>

// aprendendo a declarar ponteiros 

int main() 
{   
    // ponteiro do tipo inteiro
    int *p;
    int* c;

    // ponteiro para um registro
    struct reg *f;

    // um ponteiro para um ponteiro de um inteiro (como no caso de uma matriz de inteiros)
    int **r;
    

    return 0;
}